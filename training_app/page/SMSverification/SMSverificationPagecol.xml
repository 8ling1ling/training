<paths>
    <forward>
        <path ty:if="DATA.nextPage === 'login'" url="page/login/login.tml" class="push" />
        <path ty:if="DATA.nextPage === 'gesture'" url="page/gesture/gesture.tml" class="push" />
        <path ty:if="DATA.nextPage === 'lossAccount'" url="page/lossAccount/lossAccount.tml" class="push" />
    </forward>
    <backward>
        <path ty:if="true" class="pop"/>
    </backward>
</paths>